# WNBA Edge Detector v1.0 Agent Interview

## Product Scope

WNBA Edge Detector v1.0 provides full-game matchup intelligence for WNBA games.

It should stay model-first until the historical lab proves the signal quality.

Allowed output:

```text
Side Edge
Scoring Environment
Confidence
Edge Score
Key Factors
Agent Notes
```

Avoid:

```text
Bet
Pick
Play
Lock
Unit
Stake
Wager
Bankroll
ROI
Expected value
+EV
Best bet
Market edge
Value
```

## v1.0 Build Order

Recommended order:

```text
1. Historical Lab
2. Backtest threshold tuning
3. Current Slate
4. App selector enablement
```

Current Slate should not be enabled until a historical data source, threshold set, and validation report are available.

## Page Modes

WNBA should eventually mirror the NFL pattern:

```text
Current Slate
Historical Lab
```

Historical Lab is the first v1.0 deliverable. Current Slate comes after schedule ingestion is stable.

## Primary Signals

Side Edge:

```text
Team Edge
Pass
```

Examples:

```text
NYL Edge
CON Edge
Pass
```

Scoring Environment:

```text
High Scoring Environment
Low Scoring Environment
Neutral Scoring Environment
```

Early Edge:

```text
Model Pending
```

Early Edge remains a layout-parity placeholder in v1.0.

## v1.0 Model Inputs

Include:

```text
Rolling team strength
Rolling offensive scoring
Rolling defensive scoring
Recent margin form
Home-court context
Rolling league total baseline
Recent game-total context
```

Optional ablation:

```text
Rest context
```

Exclude:

```text
Automated injury adjustments
Player props
Lineups
Individual player ratings
Odds or market data
Referee data
Travel distance
Play-by-play matchup features
```

## Initial WNBA Thresholds

These are test defaults, not final model claims.

```text
A side edge: model margin >= 7.0
B side edge: model margin >= 5.0
C side edge: model margin >= 3.0
Scoring environment threshold: +/- 4.0 points vs rolling league baseline
Default total baseline: 164.0
Recent margin weight: 0.20
```

Confidence should be gated by minimum prior team history:

```text
A requires both teams to have at least 5 prior games
B requires both teams to have at least 3 prior games
C can appear before both teams reach 3 prior games
```

The purpose of the gate is to avoid overstating early-season confidence.

## Historical Data Contract

The first WNBA backtest path expects one row per completed game:

```text
season
game_date
away_team
home_team
away_score
home_score
```

Optional:

```text
game_id
away_rest
home_rest
```

Rest can be calculated upstream or omitted. If rest is omitted, the v1.0 model should behave as a no-rest-context ablation.

## Rolling Team Strength

Starting formula:

```text
Team Strength =
0.50 * offense edge
+ 0.50 * defense edge
+ 0.42 * season margin
+ 0.20 * recent margin
```

Definitions:

```text
offense edge = team points for average - league points per team
defense edge = league points per team - team points allowed average
season margin = team average scoring margin
recent margin = average scoring margin over the last 5 games
```

The recent margin weight starts at 0.20 because WNBA recent form can matter, but v1.0 should avoid overreacting to a short sample.

Rest principle:

```text
Rest is tested, not assumed.
If rest improves backtest quality, keep it.
If rest adds noise, remove it from v1.0.
```

## Historical Lab Metrics

Core metrics:

```text
Games
Winner accuracy
Side signal games
Side signal accuracy
Margin MAE
Total MAE
High/Low scoring signal games
High/Low scoring signal accuracy
```

Confidence breakdown:

```text
A
B
C
Pass
```

Validation splits:

```text
Home edges
Road edges
Early season
Mid/late season
Both teams with 5+ prior games
Rest advantage games
Neutral rest games
Same-season rematches
High Scoring Environment
Low Scoring Environment
```

Scoring signal accuracy should be reported on High/Low signals only. Neutral scoring environments should not inflate the scoring signal result.

Implemented helper tables:

```text
core
confidence
scoring
side_location
history
rest
```

These are generated by `wnba_agent.historical_summary_tables(results)` after the WNBA historical lab rows are built.

Run the historical report from a normalized CSV with:

```powershell
python wnba_agent.py --games-csv path\to\wnba_games.csv --season 2025 --compare-no-rest
```

To save report artifacts:

```powershell
python wnba_agent.py --games-csv path\to\wnba_games.csv --season 2025 --compare-no-rest --output-dir reports\wnba_2025
```

## Promotion Criteria

The v1.0 model is ready for Current Slate only if:

```text
Side signal accuracy beats all-game winner accuracy
A/B confidence tiers outperform C
High/Low scoring signals show useful directional accuracy
Road-edge performance does not collapse
Signal volume is large enough to be useful
Rest context is either validated or removed
```

If rest context fails to improve results, remove it from v1.0 rather than keeping it for narrative value.

## WNBA Performance Tracking Contract

WNBA tracking is intentionally separate from MLB tracking.

Do not write WNBA rows to `model_history.py` or the MLB performance dashboard.

Storage:

```text
wnba_model_history
```

Target backend is the shared Turso database with a WNBA-only table. Local SQLite
history is legacy/test plumbing only and should not be treated as the future
production-style path.

Scheduled load target:

```text
snapshot_wnba_slate.py
.github/workflows/wnba-snapshot.yml
```

When the scheduled WNBA snapshot runs without an explicit date, it records the
current ET slate and revisits the previous ET slate to settle late finals. A
manual `slate_date` dispatch records only that requested slate date.

WNBA should migrate away from Streamlit page-load history writes. The scheduled
snapshot job should own WNBA history writes, matching the MLB pattern.

UI snapshot freshness:

```text
Display `Snapshot as of: M/D/YYYY, H:MM AM/PM ET` under the current-slate
filter/result count and in the WNBA performance view header area. Derive this
from WNBA history `updated_at` / `created_at` values for the selected slate date,
not from Streamlit page-load time.
```

Snapshot rule:

```text
Create prediction snapshots only for scheduled/pre-game rows.
When a game becomes final, update only result fields on an existing snapshot.
Do not backfill final-only or in-progress-only games as if they were pregame snapshots.
```

Lock rule:

```text
Pregame snapshots may update current reads.
At game lock, official side/scoring signal fields freeze.
After lock, only result, status, outcome, and updated timestamp should change.
Performance must match the locked WNBA game card.
```

Watch/Lean rule:

```text
Official WNBA picks remain the production performance record.
Stored WNBA Watches and Leans may be graded as discovery performance once implemented.
Watch and Lean performance must be reported separately from official pick hit rate.
WNBA Watch/Lean thresholds must be defined from WNBA history, not inherited from MLB.
```

Initial WNBA discovery bands use the existing WNBA model gates:

```text
Side Official: existing WNBA side pick at absolute model margin 3.0 or higher.
Side Lean: directional winner at absolute model margin 2.25 through 2.99.
Side Watch: directional winner at absolute model margin 1.50 through 2.24.

Scoring Official: existing scoring signal at 4.0 points or more from the league baseline.
Scoring Lean: directional scoring environment 3.0 through 3.99 points from baseline.
Scoring Watch: directional scoring environment 2.0 through 2.99 points from baseline.
```

These bands classify existing model outputs only. They do not change the
official side or scoring selection gates. Each decision stores its own release
tier because a single game may have different Side and Scoring tiers.

WNBA version fields:

```text
market_version = 1.0.2-test
model_version = 1.0.0-test
```

Tracked WNBA summary fields:

```text
snapshots
completed
side_signal_games
side_signal_accuracy
scoring_signal_games
scoring_signal_accuracy
margin_mae
total_mae
```

WNBA current-slate views:

```text
All = full current slate
Top = placeholder until tracked WNBA results define a top-signal cut
Side = side-edge signals only
Scoring = non-neutral scoring-environment signals only
Early = placeholder while Early Edge remains Model Pending
Perf = WNBA-only performance dashboard
```

WNBA performance report sections:

```text
Official Performance
Discovery Performance
Diagnostics / Splits
Export
```

Official Performance includes locked official WNBA side and scoring signals
only. Discovery Performance is reserved for future WNBA watches and leans and
must not affect the official WNBA model record until validated.

The performance filters remain available whenever WNBA history exists.
`Current model` includes the active model baseline across WNBA market releases.
If the newest market release has not produced a snapshot yet, display that as a
diagnostic without hiding prior current-model history.

## Card Contract

Collapsed card:

```text
CON @ NYL

Side Edge
NYL Edge

Confidence
B

Scoring Environment
Low Scoring Environment

Early Edge
Model Pending

Key Factors
NYL has a modest rolling team-strength edge;
Projected scoring sits below the rolling league baseline
```

Expanded analysis:

```text
Projected Margin
Projected Total
League Total Baseline
Actual Margin
Actual Total
Margin Error
Total Error
Key Factors List
```

## NBA Relationship

Do not assume the WNBA model can be reused for NBA without separate validation.

Reusable:

```text
Data contract
Backtest engine shape
Agent wrapper shape
UI card contract
Validation report structure
```

Not automatically reusable:

```text
Thresholds
Home-court value
Rest effect
Recent-form weight
Scoring baseline
Signal-volume expectations
```

The WNBA model should be kicked first, then the NBA model should get separate parameters and validation.
